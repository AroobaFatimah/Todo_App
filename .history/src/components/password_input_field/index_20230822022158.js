import React from 'react'

export const PasswordInput = ({ type,
    name,
    value,
    inputID,
    defaultValue,
    label,
    helperText,
    error,
    placeholder,
    onChange }) => {
    return (
        <div>

        </div>
    )
}
